import React from 'react';

import { withStyles } from '@material-ui/core/styles';

const UnitControls = () => (<div />);

const styles = {
    root: {
        width: '100%'
    }
};

export default withStyles(styles)(UnitControls);
